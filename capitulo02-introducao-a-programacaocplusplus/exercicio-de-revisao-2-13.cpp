#include<iostream>

int main(){

// 2.13Qual das instruções C++ a seguir contém variáveis cujos valores são substituídos?

// a) cin >> b >> c >> d >> e >> f;
      // int a;
      // int b;
      // int c;
      // int d;
      // int e;
      // int f;

      // std::cout << "a) cin >> b >> c >> d >> e >> f;" << std::endl;
      // std::cin >> a >> b >> c >> d >> e >> f;
      // std::cout << "\n" << std::endl;
      // std::cout << "a = " << a << "\nb = " << b << "\nc = " << c << "\nd = " << d << "\ne = " << e << "\nf = " << f; 
      // std::cout << "\n" << std::endl;

// b) p = i + j + k + 7;
      int i;
      int j;
      int k;
      int p;
      
      std::cout << "b) p = i + j + k + 7;" << std::endl;      
      p = i + j + k + 7;
      

// c) cout << “variables whose values are replaced”;
      std::cout << "variables whose values are replaced"<< std::endl;
      

// d) cout << “a = 5”;
      std::cout << "a = 5" << std::endl;

}